import React from 'react'
import styled from 'styled-components'

const LogoStyle = styled.p`
  background: transparent;
  font-weight: 900;
  font-size: 1.5rem;
  color: black;
`

const Logo = () => <LogoStyle>FURWERS</LogoStyle>

export default Logo